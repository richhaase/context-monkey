{{!-- Guidance for manual execution when delegation is unavailable. Accepts optional indent parameter. --}}
{{#if indent}}{{indent}}{{/if}}- Use available workspace tools (git, grep, file reads) to gather evidence.
{{#if indent}}{{indent}}{{/if}}- Provide the complete analysis directly in chat since {{agent.name}} cannot delegate this step yet.
